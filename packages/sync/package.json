{
  "name": "@farm-erp/sync",
  "version": "0.1.0",
  "private": true,
  "description": "Shared wire-format types for Farm ERP sync protocol (api ↔ mobile)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .tsbuildinfo"
  },
  "license": "UNLICENSED"
}
